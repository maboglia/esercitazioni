# 🐍 **Corso Python**

## 🧠 Esercizi su Funzioni e Controllo del Flusso

---

## 🔢 Massimo tra due numeri

Scrivi una funzione che riceva in ingresso **due numeri** e **restituisca il valore massimo** tra i due.

---

## 🖥️ Orientamento finestra (Portrait o Landscape)

Scrivi una funzione che riceva in ingresso **larghezza** e **altezza** di una finestra e restituisca:

* `"landscape"` se la larghezza è maggiore dell’altezza
* `"portrait"` se l’altezza è maggiore della larghezza

---

## 🔁 FizzBuzz

Scrivi una funzione che implementi l’algoritmo **FizzBuzz**.

La funzione riceve un **numero intero** e restituisce:

* `"fizz"` se il numero è divisibile per 3
* `"buzz"` se il numero è divisibile per 5
* `"fizzbuzz"` se il numero è divisibile sia per 3 che per 5
* il numero stesso negli altri casi

---

## 🚗 Controllo del limite di velocità

Scrivi una funzione che controlli la velocità di un veicolo:

* Fino a **50 km/h** → stampa `"OK"`
* Oltre i 50 km/h, **ogni 5 km/h in più** → stampa `"Punti in meno: X"`
* Oltre i **130 km/h** → stampa `"Patente ritirata"`

---

## 🔢 Numeri pari e dispari

Scrivi una funzione che, dato un **numero intero**, stampi per ogni numero da **0 fino al numero fornito** se è:

* `"pari"`
* `"dispari"`

---

## 📦 Proprietà di un oggetto

Scrivi una funzione che, dato un **dizionario Python**, stampi a video **tutte le sue chiavi e i relativi valori**.

---

## 📊 Calcolo del giudizio

Scrivi una funzione che riceva in ingresso una **lista di voti** e stampi il **giudizio finale** in base alla **media** secondo la seguente tabella:

| Media voto | Giudizio      |
| ---------: | ------------- |
|       < 18 | insufficiente |
|  18 – < 21 | sufficiente   |
|  21 – < 24 | discreto      |
|  24 – < 27 | buono         |
|  27 – < 29 | distinto      |
|         30 | ottimo        |

---

## ⭐ Asterischi

Scrivi una funzione che, dato un **numero intero**, stampi una **figura triangolare** con:

* un numero di righe pari al numero fornito
* un numero crescente di asterischi per ogni riga

Esempio con input `4`:

```
*
**
***
****
```

---

## 🔍 Numeri primi

Scrivi una funzione che, dato un **numero intero**, **restituisca tutti i numeri primi** **minori** del numero fornito in input.
