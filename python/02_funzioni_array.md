 # 🐍 **Corso Python**

## 📦 Esercizi su Funzioni e Liste

---

## 🔢 Intervallo di numeri interi

Scrivi una funzione che riceva in ingresso **due numeri interi consecutivi** e **restituisca una lista** contenente **tutti i numeri interi compresi tra i due estremi** (inclusi).

---

## 🔍 Numero presente nella lista

Scrivi una funzione che, data una **lista di numeri**, verifichi se un **numero intero** passato come argomento è **presente nella lista**.

La funzione deve restituire `True` o `False`.

---

## 🚫 Escludi numeri da una lista

Scrivi una funzione che riceva:

* una **lista di numeri**
* una **lista di numeri da escludere**

e restituisca una **nuova lista** contenente solo i numeri della prima lista **non presenti** nella seconda.

---

## 🔁 Spostamento di un elemento nella lista

Scrivi una funzione che riceva:

* una **lista di numeri**
* un **indice**
* un **offset**

La funzione deve spostare l’elemento che si trova all’indice indicato nella nuova posizione calcolata con l’offset.

Se la nuova posizione non è valida, il programma deve stampare:

```
Posizione non valida
```

e non modificare la lista.

---

## 🔢 Conteggio delle occorrenze

Scrivi una funzione che, data una **lista di numeri** e un **numero da cercare**, restituisca il **numero di occorrenze** del valore all’interno della lista.

---

## 📈 Valore massimo in una lista

Scrivi una funzione che riceva una **lista di numeri** e restituisca il **valore massimo** contenuto nella lista.

---

## ➕➖✖️➗ Operazioni aritmetiche tra liste

Scrivi un programma che:

1. Crei **due liste** di **10 numeri interi casuali** compresi tra **1 e 10**
2. Chieda all’utente di scegliere un’operazione aritmetica tra:

   * `addizione`
   * `sottrazione`
   * `moltiplicazione`
   * `divisione`
3. Esegua l’operazione **elemento per elemento** tra le due liste
4. Salvi ogni risultato in una **terza lista**
5. Stampi la lista dei risultati

⚠️ **Nota:** gestire correttamente la divisione per zero.

